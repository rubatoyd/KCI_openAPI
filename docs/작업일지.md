# 작업일지 — kci-openapi-mcp

> 세션별 수행·결정·다음 할 일 기록 (최신 위). 자매 프로젝트 `scienceon-mcp` 및
> 연계 연구 `투고논문/학부모 학술동향`의 작업일지 컨벤션을 따른다.

---

## 2026-08-11 (9) — GitHub 계정명 변경(`rubato103` → `rubatoyd`) 반영 · MCP 기동 실패 복구

### 증상
Claude Desktop 의 `kci` 서버가 오늘 3회 연속 죽었다(`mcp-server-kci.log`, 11:04·11:10·11:11 KST).

```
Updating https://github.com/rubato103/KCI_openAPI (HEAD)
× Failed to resolve `--with` requirement
╰─▶ Git operation failed
```

8/5~8/6 로그에는 없던 오류이고, 발생 시점이 계정명 변경과 일치한다.

### 원인
`uvx --from git+…` 는 **서버를 띄울 때마다 원격 HEAD 를 재해석**한다. 따라서 저장소 주소가
런타임 단일 장애점이다. 계정명이 바뀌면서 구 주소 해석이 실패했다. GitHub 리다이렉트가
복구된 뒤로는 구 주소도 다시 응답하지만(`git ls-remote` 양쪽 모두 `699d7a4`), 리다이렉트에
의존해 방치할 이유가 없다.

### 수행
- **주소 갱신** — `.mcp.json` · `claude_desktop_config.json` · `git remote origin` ·
  `pyproject.toml` · `server.json` · `README.md`(`mcp-name` 주석 포함) · `mcpb/manifest.json` ·
  `packaging/binary/manifest.json` · `docs/DIRECTORY_SUBMISSION.md` · `docs/SUBMISSION.md`.
  치환은 `rubato103(?!@gmail\.com)` 로 제한해 **연락처 이메일은 보존**했다.
- **본 작업일지의 과거 기록은 보존** — 당시 사실이므로 소급 수정하지 않는다.
- **검증** — 신주소로 `uvx --refresh` 콜드 스타트 재현: 39패키지 설치 후 `initialize` 응답 정상.
  `kci_status` 실호출 정상(OAI Identify + REST 키 유효). 테스트 31건 통과.

### 2차 점검 — Claude Code 쪽 잔존 (같은 세션에서 추가 수정)
`.mcp.json` 만 고치면 끝이 아니었다. MCP 등록 지점이 **3곳**이고 서로 독립이다.

| 지점 | 대상 | 상태 |
|------|------|------|
| `.mcp.json` (프로젝트 스코프) | 본 저장소 `kci` | ✅ 1차에서 해결 |
| `claude_desktop_config.json` | Claude Desktop `kci` | ✅ 1차에서 해결 |
| **`~/.claude.json`** (사용자 전역, 프로젝트별 local 스코프) | `투고논문/학부모 학술동향` 의 `kci`·`scienceon` | ⚠️ **구주소로 남아 있었음 → 수정** |

- 연계 연구 프로젝트에 `kci`(구주소)와 **`scienceon`**(`scienceon-mcp @ git+…/rubato103/scienceon-mcp`)이
  local 스코프로 직접 등록돼 있었다. 계정 변경은 **모든 저장소**에 걸리므로 자매 프로젝트도 동일하게 깨진다.
  둘 다 신주소로 갱신(`~/.claude.json.bak-20260811` 백업 보관).
- ⚠️ **`~/.claude.json` 은 JSON 파싱 후 재직렬화하면 안 된다** — 대소문자만 다른 중복 경로 키
  (`c:/…KCI openAPI` 와 `C:/…KCI openAPI`)가 실재해 한쪽이 조용히 소실된다. 원문 문자열 치환만 사용했다.

### 확인된 별건 (미해결)
- **레지스트리 네임스페이스 이관** — `server.json` 을 `io.github.rubatoyd/…` 로 바꿨다.
  발행 워크플로가 GitHub OIDC 로 계정 소유를 검증하므로 구 네임스페이스로는 발행이 불가능하다.
  기존 발행분(`io.github.rubato103/…` v0.1.3)은 고아로 남는다. 다음 태그 푸시 시 새 항목 생성.
- **프로젝트 내 `.venv/` 파손** — `pyvenv.cfg` 의 home 이 `C:\Users\rubat\…`(타 PC 프로필)를 가리켜
  `uv run` 이 exit 103. OneDrive 동기화로 유입된 것으로 보인다. **MCP 동작에는 무관**(uvx 격리환경).
  `UV_PROJECT_ENVIRONMENT` 는 §2 기재와 달리 실제로는 미설정 상태.
### 3차 — 자매 저장소 `scienceon-mcp` 점검 (같은 세션에서 이어서 수행)
계정 변경은 **모든 저장소**에 걸리므로 `../scienceon` 도 같은 점검을 돌렸다. 주소 12곳을 갱신
(`git remote`·`pyproject.toml`·`README.md`·`mcpb/manifest.json`·`mcpb/README.md`·`CLAUDE.md`).
`server.json` 이 없어 **레지스트리 미발행** → 네임스페이스 이관 이슈는 kci 에만 해당.

그 과정에서 **주소와 무관한 기동 불능 버그**가 드러났다.

- 🔴 **scienceon 서버가 아예 기동되지 않고 있었다** — `mcp>=1.2.0` 에 **상한이 없어** mcp 2.0 으로
  해석되고, 2.0 은 `mcp.server.fastmcp` 를 제거했다. `ModuleNotFoundError: No module named
  'mcp.server.fastmcp'`. **kci 가 v0.1.1 에서 이미 겪고 고친 바로 그 버그**인데 자매 프로젝트에는
  처방이 전파되지 않았다. `mcp>=1.2.0,<2` 로 고정 → `initialize` 응답·pytest 11종 통과 확인.
  ⚠️ `uv run --with "… @ git+…"` 는 **uv.lock 을 보지 않고 매번 새로 해석**한다. lock 에 1.28.0 이
  박혀 있어도 무의미했다 — 원격 HEAD 기반 MCP 등록에서는 **pyproject 의 상한만이 유효한 방어선**이다.
- ⚠️ **`.venv` 파손이 두 저장소 공통** — 둘 다 `pyvenv.cfg` home 이 `C:\Users\rubat\…`.
  scienceon 검증용으로 `C:/Users/user/.venvs/scienceon-mcp` 를 새로 만들어 우회했다.
- ⚠️ **일시적 설치 실패** — 최초 휠 추출 시 `os error -2147024891`(액세스 거부)가 서로 다른 패키지에서
  두 번 났다가 재시도로 해소. 실시간 검사 충돌로 추정. 캐시된 뒤로는 재현되지 않는다.
- ✅ **scienceON push 완료** — MCP 등록분이 원격 HEAD 를 받아오므로 push 전에는 로컬 수정이 무의미했다.
  push 후 `uvx --refresh` 로 실제 기동 재현 확인.

### 4차 — 반복 개선 (같은 세션, 항목 6종 전수 처리)
주소 정리에서 시작해 "더 볼 것이 없을 때까지" 훑은 결과. 상세는 `../scienceon/CLAUDE.md` 참조.

| # | 항목 | 결과 |
|---|------|------|
| 1 | scienceON 표기 통일 | 저장소 rename + 서버키·도구명·URL 전량. GitHub 는 **대소문자 무시 조회**라 구 URL 유효 |
| 2 | **scienceON 조용한 절단 제거** | `search_meta`/`search_terms_meta`/`search_groups_meta` 신설. 회귀 11→16 |
| 3 | scienceON truststore 정식화 | 등록 명령줄에만 있어 `.mcpb` 경로가 교육망에서 실패하던 것을 의존성으로 이관 |
| 4 | KCI v0.1.4 준비 | 버전 5곳 상향 + **문서 거짓 서술 정정**(발행 네임스페이스) |
| 5 | scienceON 릴리스 파이프라인 | `publish-mcp.yml`·`packaging/binary`·`server.json` 이식, `mcpb validate` 통과 |
| 6 | 기동 시 stderr 경고 | pydantic-settings 2.14 발 무해 경고로 확정. **핀하지 않고 기록만** |

특히 ②는 kci 가 v0.1.3 에서 고친 결함이 자매 프로젝트에 전파되지 않은 사례였다. **한쪽에서 고친
결함은 다른 쪽에도 있다고 가정하고 확인할 것** — 이번 세션에서 이 패턴이 두 번(mcp 상한, 조용한 절단)
반복됐다.

④에서는 계정명 블랭킷 치환이 `docs/SUBMISSION.md` 에 "신 네임스페이스 v0.1.3 발행됨(검색 가능)"이라는
**사실과 다른 서술**을 만들어냈다. 기계적 치환은 *지칭 대상이 과거 사실인 문장*을 현재 사실로 둔갑시킨다.
치환 후에는 문장 단위 확인이 필요하다.

⏭️ **두 저장소 모두 태그 미푸시** — 레지스트리 발행·릴리스는 공개 행위이므로 별도 확인 후 진행.

### 다음
① `.mcpb` 재빌드 + 새 태그 푸시로 신 네임스페이스 발행 → ② 파손된 `.venv/` 정리 및
`UV_PROJECT_ENVIRONMENT` 프로젝트 스코프 고정 → ③ 구 릴리스 자산 링크 점검.

---

## 2026-08-04 (8) — MCP 실사용 점검에서 드러난 결함 3종 수정 · v0.1.3

### 수행
Claude Desktop/Code 등록 후 **7개 도구 전수 실사용 점검**. 그 과정에서 나온 결함을 고쳤다.

- **① 조용한 절단 제거(최우선)** — '학부모' 수집이 3,000 → 6,000 건으로 **정확히 max_records 와 일치**
  했는데 절단 사실이 어디에도 표시되지 않았다. 축별 `total` 을 직접 실측하기 전까지 불완전 코퍼스를
  완전한 것으로 오인할 수 있었다. `search_meta` / `search_terms_meta` 를 신설해
  `total·fetched·truncated·axes·union_upper_bound·warning` 을 반환하고, `kci_search`·`kci_collect`
  응답에 노출. 기존 `search`/`search_terms` 는 얇은 래퍼로 남겨 호환 유지. 회귀 테스트 7건 추가.
- **② `kci_status` 문구 정정** — "라이브 미검증" → "2026-08-04 7개 도구 라이브 검증 완료".
- **③ 키워드 비대칭 문서화** — `articleSearch` 는 `keyword=` **검색은 지원하나 응답에 키워드를 싣지 않는다**.
  원본 XML 24개 필드에 키워드·ISSN·UCI 자체가 없다(파서 결함 아님). `articleDetail` 에는
  `<keyword-group>`·`<issn>`·`<kci-registration>`·`<fwci>`·`<referenceInfo>` 가 모두 있다.
  도구 description 과 CLAUDE.md §5 에 명시.

### 실측 (학부모 코퍼스, 2026-08-04)
| 항목 | 값 |
|---|---|
| 제목축 total / 키워드축 total | 4,764 / 4,795 (abstract 단독검색 불가) |
| 합집합 | **6,298건** (겹침 3,261) — 키워드축 전용 유입 1,534편 |
| 연도 범위 | 1992 ~ 2026 |
| 결측 | keywords·issn·uci **100%** · doi 47.1% · abstract 6.0%(376건) |
| 초록 | 국문 보유 75.3% · 국영문 동일 19.6%(영어 작성 논문) |
| 분류 상위 | 교육학 1,090 · 학제간연구 437 · 기타사회과학 355 |

### 발견/결정
| 항목 | 내용 |
|---|---|
| 상한값과 일치하는 결과 | **절단 신호로 간주**해야 한다. 파이프라인 어디서든 `n == max_records` 는 경고 대상 |
| 검색축 이중성 | `terms` 는 title·keyword 두 축 합집합 — 재현율에는 유리하나 코퍼스 경계 기술이 달라진다 |
| 키워드 확보 비용 | 6,298건 건별 `articleDetail` ≈ 45분~1시간. STM 토픽 라벨링을 하려면 불가피 |

### 다음
- 키워드 보강 패스(전건) 후 ScienceON 621편과 DOI/제목 정규화 매칭 → 완전성 교차검증·초록 백필.

---

## 2026-08-04 (7) — mcp SDK 2.0 회귀 대응 · REST 라이브 검증 · v0.1.1

### 수행
- **회귀 원인 규명**: `mcp` SDK **2.0.0** 에서 `mcp.server.fastmcp` 제거(→ `mcp.server.MCPServer` 체계).
  `pyproject.toml` 의 `mcp>=1.2.0` 이 상한 없이 2.0 으로 해석되어 `uvx --from git+…` 기동이
  `ModuleNotFoundError` 로 실패. **`>=1.2.0,<2` 상한 고정**(pyproject·uv.lock)으로 해결, push 후 재현 불가 확인.
  - 영향 범위: uvx 경로(README·`.mcp.json`·수동 config·`~/.claude.json`) 전부. **`.mcpb` 번들·로컬 `.venv` 는 무영향**(빌드시점 고정).
- **REST 라이브 검증 완료** — MCP stdio 로 7개 도구 전수 실호출 성공.
  `kci_detail`(`arti_id=ART003047608`, 분류 `사회과학 > 교육학`) · `kci_references` ·
  `kci_journal_citation`(2023년 50저널, `impactFactor`/`exImpactFactor`/`selfCitedRate`) ·
  `kci_collect`(라우터가 `rest` 선택 → xlsx·json 산출). → CLAUDE.md §6 "라이브 미검증" 단서 해소.
- **Claude Desktop 등록**: 전용 venv `C:/Users/rubat/.venvs/kci-openapi-mcp` + `python -m kci_mcp.server`
  (uvx 대신 직접 지정 — 상류 해결 드리프트 차단, scienceon 과 동일 패턴). `shrimp-task-manager` 제거.
- **v0.1.1 릴리스**: pyproject·`__init__`·mcpb/manifest·packaging/binary/manifest·server.json·uv.lock 버전 동기화.
- **CI 릴리스 파이프라인 수정 → v0.1.2**: v0.1.1 릴리스에 OS별 자체완결 `.mcpb` 3종이 첨부되지 않은 것을 확인.
  원인은 **GITHUB_TOKEN 이 만든 release 는 다른 워크플로의 `release: published` 를 트리거하지 않는다**는
  GitHub Actions 재귀 방지 규칙 — `build-mcpb.yml` 이 v0.1.0 때도 수동 dispatch 로만 돌았던 이유.
  `publish-mcp.yml` 을 **binaries(매트릭스) → publish(needs)** 2-job 구조로 통합해 태그 푸시 한 번으로
  빌드·릴리스 첨부·레지스트리 발행이 끝나도록 했고, 빌드 전 `import mcp.server.fastmcp` 가드를 추가했다.
  `build-mcpb.yml` 은 과거 릴리스 보정용 수동 dispatch 전용으로 축소.

### 발견/결정
| 항목 | 내용 |
|---|---|
| 상한 없는 의존성 | 상류 major 릴리스가 소스 미변경 상태의 재현빌드를 깨뜨림 → **런타임 SDK 는 major 상한 필수** |
| KCI 방화벽 조건 | 안내문은 "해외 IP 차단"이나 실제 조건은 **User-Agent 필터** — `curl` 기본 UA 는 400, `requests` UA 는 200. 원격/클라우드 환경에서도 REST·OAI 정상 |
| 배포 경로 이중화 | uvx(자동 최신·네트워크 의존) vs 고정 venv/`.mcpb`(재현성) — 개인 상시 사용은 후자, 공개 배포는 전자 |

### 다음
- `kci_collect` 혼용 교차검증·초록 백필을 학부모 코퍼스에 적용(ScienceON 621편 대비 완전성 점검).
- pytest 24 통과 유지 + CI 매트릭스 재빌드 결과 확인.

---

## 2026-06-23 (6) — 공식 레지스트리 발행 · 양 클라이언트 등록 · 자체완결 .mcpb(크로스플랫폼)

### 수행
- **공식 MCP 레지스트리 발행 완료**: `io.github.rubato103/kci-openapi-mcp` v0.1.0 (검색됨).
  - 경로: **mcpb 방식**(PyPI 불필요) — `.mcpb`(GitHub Release) + `mcp-publisher github-oidc`. CI(`publish-mcp.yml`)가 태그 푸시 시 자동.
  - server.json(2025-12-11 스키마) 검증 통과 후 발행(immutable). repo **public** 전환.
- **양 클라이언트 등록**: Claude Code `.mcp.json`(uvx-from-git, 라이브 검증) + Claude Desktop `mcpb/manifest.json`.
  ⚠️ `.mcp.json` 초기 `uv run` 이 **프로젝트 내 .venv 생성(지침위반)** → **uvx 로 수정**(전역 캐시).
- **도구 annotations**(readOnly/openWorld) + **디렉터리 심사 가이드** `docs/SUBMISSION.md`.
- **자체완결 `.mcpb`(Python·uv 불필요)**: Claude Desktop은 Node만 번들 → PyInstaller 단일 실행파일로 OS별 빌드.
  - Windows 바이너리 로컬 빌드 후 **MCP 프로토콜 + OAI 실호출 검증**(truststore도 frozen exe에서 SSL 통과).
  - CI 매트릭스(`build-mcpb.yml`, win/mac/linux) → v0.1.0에 `…-win-x64/…-macos-arm64/…-linux-x64.mcpb` 첨부.

### 발견/결정
| 항목 | 내용 |
|---|---|
| 레지스트리≠Desktop검색 | 레지스트리는 메타데이터 저장소; Desktop 인앱 디렉터리는 Anthropic 큐레이션 → 별도 심사 신청 필요 |
| Desktop Python | Desktop은 **Node만 번들**, Python 미포함 → uv/python 타입은 시스템 Python 필요. 자체완결=PyInstaller binary뿐(OS별) |
| PyInstaller | `--collect-all mcp` 는 `mcp.cli`(sys.exit) 때문에 실패 → `--collect-submodules mcp.server` + `--exclude-module mcp.cli` |
| venv 지침 | `.mcp.json` 은 uvx(전역캐시)로 — 프로젝트 내 venv 금지 |

### 상태
- ✅ 레지스트리 발행 · 양 클라이언트 · 자체완결 3-OS .mcpb · pytest 24 · 감사 버그9 수정 · annotations
- ⏭️ (선택) Anthropic 디렉터리 등재 신청(사람-게이트, SUBMISSION.md) · `.venv` 56K 잔여는 Claude 재시작 시 해제

---

## 2026-06-22 (5) — MCP 재초점화: 정밀테스트 · 마감 감사 · 양 클라이언트 패키징 · 공식 레지스트리 준비

> 프로젝트 본질은 **MCP 개발**(코퍼스 수집은 기능 테스트였음)으로 재초점화.

### 수행
- **MCP 프로토콜 스모크 테스트**(scripts/mcp_smoke.py): stdio 기동→initialize→list_tools(7종)→call_tool 실제 프로토콜 검증 통과.
- **마감 감사 워크플로**(ultracode, 19 에이전트, 7영역 병렬 + 적대적 검증): 확정 버그 9건 + 개선 다수.
- **버그 수정**:
  - 🔒 인증키 URL 예외 누출 차단(_call 에서 requests 예외→KciError, raise_for_status 미사용, 상태코드만)
  - 한글 깨짐 방지(charset 부재 시 r.encoding=apparent_encoding/utf-8)
  - check_rest_error: outputData 부재=에러로 ParseError(화이트리스트·무음통과 제거), _parse_xml 로 ET.ParseError 매핑
  - dedup_key: 항상 str(id:/doi:/tt:) + DOI URL접두 정규화 / matches(빈필터)=전부통과
  - 네트워크 예외 재시도, 4xx 매핑, citation 페이지네이션, OAI date 정규식, metadata 직속자식 매칭
  - server: `@_safe` 데코레이터로 **모든 도구가 예외 누수 없이 dict 반환**
- **패키징**: `mcpb/manifest.json`(Desktop) · `.mcp.json`(Claude Code) · entry point `kci-openapi-mcp` 추가(uvx).
- **공식 레지스트리 준비**: `server.json`(2025-12-11 스키마, io.github.rubato103/kci-openapi-mcp) · README `mcp-name` 주석 ·
  `.github/workflows/publish-mcp.yml`(PyPI Trusted Publishing OIDC + mcp-publisher github-oidc).
- 검증: pytest **24건** + 프로토콜 스모크 + `uv build`(sdist/wheel) 통과. PyPI 명 `kci-openapi-mcp` 사용가능 확인.

### 다음 (발행)
1. repo **public 전환** → 커밋·푸시 → `git tag v0.1.0 && git push --tags` 로 CI 발행 트리거.
2. **user-gated**: PyPI Trusted Publishing 1회 설정(pypi.org에서 이 repo/워크플로 신뢰 게시자 등록).
3. CI 가 PyPI 게시 → mcp-publisher(OIDC)로 레지스트리 발행.

---

## 2026-06-22 (4) — 인증키 수령 · REST 라이브 검증 · 경계선지능/느린학습자 코퍼스 수집

### 수행
- **인증키 수령**(한국연구재단 연구데이터분석팀) → `.env` 저장(gitignore 확인, 커밋 안전).
- **REST 5종 라이브 검증 완료**: articleSearch/articleDetail/referenceSearch/citation/citationDetail 정상.
- **경계선지능·느린학습자 코퍼스 수집(REST title∪keyword)**: **311편 / 10초**, output/borderline_slow/.
  - 용어×필드: 경계선지능 t177/k190, 느린학습자 t61/k81, slow learner t10/**k52**, borderline intelligence t63/k76, 경계선지적기능 t37/k44
  - 발행연도 1998~2026(2025:89, 2023:38, 2022:20…), **국문초록 304/311·영문초록 306/311** 보유.

### 검증된 검색식 사실 (docs §1-1-a "검증됨" 갱신)
| 항목 | 결과 |
|---|---|
| `title` | 제목검색(토큰화). 띄어쓰기 무관(느린학습자=느린 학습자=느린학습=61). 20중 19 제목포함 |
| **`keyword` 단독검색 가능** ★ | keyword=경계선지능(타이틀 없이) 190 > title 177. 모든 용어에서 keyword가 title보다 많음 → **title∪keyword 합집합 필수** |
| `abstract` 단독 | 0건(무효) |
| 연산자 | 없음 → 변형어·필드별 개별검색 합집합 |
| keyword 출력 | articleSearch 응답엔 키워드 미포함(0) — 키워드는 articleDetail 전용 |

### 코드 반영
- `client.search_terms(fields=("title","keyword"))` 다중필드 합집합, `search/search_page` field 파라미터화. pytest 22 유지.

### 다음 할 일
1. (선택) articleDetail 보강으로 키워드 확보, referenceSearch로 인용연결.
2. **학부모 코퍼스 교차검증·초록 백필**(KCI 국문초록 보유율 높음 → NART 영문 백필원으로 유효 확인됨).
3. ScienceON 코퍼스(경계선지능/느린학습자)와 KCI 311편 교차(arti_id/DOI/제목).

---

## 2026-06-22 (3) — GitHub 공개 · 기능 점검(pytest) · 시범수집 · 검색식 확인

### 수행
- **GitHub**: private `rubato103/KCI_openAPI` 생성·푸시(인증키/PDF 미포함 위생 확인). MIT LICENSE 추가.
- **기능 점검**: pytest 스위트(REST/OAI 파서·라우터·내보내기·무키·REST search_terms) — **22건 통과**.
  REST 파서는 공식 예제 XML로 오프라인 검증(키 발급 전 미리 확정).
- **경계선지능·느린학습자 시범 수집(OAI)**: 2025 H1 ~8,000건 스캔 **매칭 0**.
  원인: ① 주제 희소 ② OAI datestamp=수정일이라 최근 갱신분만 → 과거 발행분 누락. **OAI는 희소·전기간 키워드 코퍼스에 부적합** 결론.
- **REST 코퍼스 경로 보강**: `client.search_terms`(변형어 title 개별검색→arti_id 합집합 + 연도필터 + contains 후처리),
  `Article.matches`(REST/OAI 공통), `kci_collect`/CLI `collect`/config `terms` 연동.
- **검색식 확인(KCI 문서)**: KCI articleSearch 는 **필드 내 연산자(AND/OR/*/"" ) 없음** → 변형어 OR=개별검색 합집합이 유일.
  **title 필수 = 사실상 제목검색**(키워드/초록 단독검색 불가). docs/KCI_API_GUIDE.md §1-1-a 신설.

### 결정 / 발견
| 항목 | 내용 |
|---|---|
| 코퍼스 도구 | 경계선지능/느린학습자 등 희소·전기간 → **REST search_terms(변형어 합집합)** 가 정답. OAI는 보완·교차검증용 |
| 검색식 | 연산자 없음·title 필수·다중파라미터 AND. 부분일치 추정(라이브 검증 요) |
| 산출물 | `scripts/pilot_harvest.py`, `config/borderline_slow.yaml`(terms+year+contains), `.gitattributes` |

### 다음 할 일
1. **인증키 발급 대기 중** → 도착 즉시 `kci collect --config config/borderline_slow.yaml`(backend=auto→REST)로 본수집.
2. REST 라이브 검증(§1-1-a 열린질문: title 부분일치/토큰화, keyword 병용, 띄어쓰기·영문 처리) → 가이드 갱신.
3. 학부모 코퍼스 교차검증·초록 백필.

---

## 2026-06-22 (2) — OAI-PMH 발견 · 혼용 MCP 구현 · OAI 라이브 검증

### 수행
- **KCI OAI-PMH 활용가이드.pdf(19p) 분석·마이그레이션** → `docs/KCI_OAI_PMH_GUIDE.md`.
  핵심 발견: KCI는 **두 인터페이스** — REST(키 필요) + **OAI-PMH(`/oai/request`, 무인증 대량수확)**.
- **혼용 아키텍처 설계** → `docs/ARCHITECTURE.md` (공통코어 + REST/OAI 두 클라이언트 + 라우터).
- **`src/kci_mcp/` 전체 구현**: config·models·parser(REST+oai_dc+oai_kci)·oai_client·client·router·exporters·server·cli.
  pyproject + 외부 venv(uv sync) + `.env.example` + `config/search.example.yaml`.
- **OAI 라이브 검증 완료(무인증)**: Identify/ListSets/ListMetadataFormats/ListRecords(oai_kci·oai_dc)/GetRecord +
  `학부모` 로컬필터 + exporters(csv/json) 동작 확인. router 4케이스 정상.

### 결정 / 발견
| 항목 | 내용 |
|---|---|
| 인터페이스 | REST + **OAI-PMH 둘 다 지원**. `kci_collect`가 요청·키유무로 자동 라우팅 |
| 무인증 경로 | OAI로 **키 없이** 코퍼스 수집·MCP 개발·검증 가능 (키워드 검색만 불가 → 로컬 필터) |
| MCP 도구 | kci_status/search/detail/references/journal_citation/**harvest**/**collect** |
| ⚠️ datestamp | **저장/수정일**(발행일 아님) — `from/until`은 갱신일 필터. 발행연도는 `pub_year`로 로컬 처리 |
| ⚠️ 초록 | 일부 논문 **국문 없이 영문(original)만** → 백필 시 lang 분기 필수 |
| ⚠️ 교육망 | 학교/교육청 SSL 인터셉션(자체서명 CA) → `truststore`로 OS 신뢰저장소 사용(검증 유지). `KCI_OS_TRUST=0` 비활성. 서울 초등 교실망 통과 확인 |

### 다음 할 일
1. **KCI 인증키 발급** → REST(search/detail/references/citation) 라이브 검증 → `KCI_API_GUIDE.md` 갱신
2. `kci_collect` 혼용 교차검증·초록 백필을 **학부모 코퍼스**에 적용 (REST∪OAI, artiId/DOI 중복제거)
3. tests/CI · README 사용법 · `.mcpb` 패키징

---

## 2026-06-22 — 프로젝트 착수 (학부모 학술동향 세션에서 분기)

> 원본 세션 트랜스크립트: `~/.claude/projects/<KCI 인코딩>/853440f3-044c-49f7-a9de-3ca33d9c7b67.jsonl`
> (학부모 세션에서 복사 이식 — `claude --resume`로 문맥 승계 가능)

### 수행
- **KCI Open API Service 활용가이드.pdf(19p) 분석.** 한글 폰트 인코딩 깨짐 → pypdf로 전체 텍스트 추출·복구.
- KCI = **한국연구재단(NRF) Korea Citation Index**, 현재 학부모 프로젝트가 쓰는 **ScienceON(KISTI)과 별개**의 DB·API임을 확인.
- **MCP 개발 결정** → `coding project\KCI openAPI\` 폴더로 관련 내용 마이그레이션.
- 산출물:
  - `docs/KCI_API_GUIDE.md` — **5개 API 전체 복구 명세**(파라미터·응답 XML 구조·예제·에러 9종·구현메모)
  - `CLAUDE.md` — 프로젝트 지침(scienceon-mcp 미러)
  - `README.md`, `.gitignore`
  - `reference/KCI Open API Service 활용가이드.pdf` — 공식 원본(gitignore)
- **세션기록 마이그레이션**: 작업일지(A, 이 문서) + 원본 트랜스크립트(B, `853440f3…jsonl`) KCI 폴더로 복사.

### 결정
| 항목 | 결정 | 사유 |
|---|---|---|
| 아키텍처 | `scienceon-mcp`(../scienceon) 미러 — 공용코어 + MCP서버 + CLI | 완성된 자매 프로젝트 재사용 |
| 인증 | 평문 `key` 쿼리 1개 (AES/토큰/공인IP 불필요) → `auth.py` 생략 | KCI는 ScienceON과 달리 토큰 발급 없음 |
| 의존성 | mcp·requests·openpyxl·python-dotenv·pyyaml (**pycryptodome 제거**) | 암호화 불필요 |
| MCP 도구셋 | `kci_status` / `kci_search` / `kci_detail` / `kci_references` / `kci_journal_citation` / `kci_export` | scienceon 도구 대응 |
| 환경변수 | `KCI_API_KEY` | |
| venv | 클라우드 밖 `C:/Users/user/.venvs/kci-openapi-mcp` (`UV_PROJECT_ENVIRONMENT`) | OneDrive 동기화 충돌 방지 |

### 연계 (학부모 학술동향, ScienceON 621편 STM)
KCI는 학부모 코퍼스의 ① **국문 초록 백필원**(articleSearch 응답에 국문초록 포함 — NART 영문 206편 대상)
② **완전성 교차검증**(ScienceON↔KCI 수록범위 차이) ③ 연구분야 분류·인용지수 확보 후보.

### 다음 할 일
1. **KCI 인증키 발급** (open.kci.go.kr Open API 신청)
2. `src/kci_mcp/` 구현 — client/parser/models/server/cli + pyproject (scienceon 베이스)
3. 각 API **라이브 검증** → `docs/KCI_API_GUIDE.md` "검증됨" 갱신
4. 학부모 코퍼스 교차검증·초록 백필 적용

### 열린 질문 (키 발급 후 실측)
- `title` 필수 제약 하에서 `keyword`/`abstract` 단독·조합 검색이 동작하는가? (키워드 코퍼스 회수 전략)
- `articleSearch`/`articleDetail` 응답에 **국문 초록이 실제로 채워져 있는가** — NART 영문 논문 백필 가능성 직접 검증.
- 응답 XML의 전각 따옴표(`〞`)는 PDF 폰트 문제로 추정 — 실제 응답이 표준 `"`인지 확인.
